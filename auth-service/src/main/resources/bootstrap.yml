server:
  port: 9999
  context-path: /uaa
spring:
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        serviceId: configuration-service
  application:
    name: auth-service